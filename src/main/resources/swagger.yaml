openapi: 3.0.1
info:
  title: My API
  description: A sample API for testing purposes
  version: 1.0.0
paths:
  /hello:
    get:
      summary: Get a greeting message
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Hello, world!